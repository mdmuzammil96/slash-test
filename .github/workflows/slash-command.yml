name: Slash Command Demo

on:
  issue_comment:
    types: [created]

jobs:
  demo:
    if: |
      github.event.issue.pull_request &&
      github.event.comment.body == '/run-tests'
    runs-on: ubuntu-latest

    steps:
      - name: Say hello
        run: |
          echo "Slash command detected!"
          echo "Running basic checks..."
          sleep 5
